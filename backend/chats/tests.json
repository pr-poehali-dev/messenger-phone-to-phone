{
  "tests": [
    {
      "name": "Messages without chat_id returns 400",
      "method": "POST",
      "path": "/",
      "headers": {"X-Auth-Token": "invalid_token_test"},
      "body": {"action": "messages"},
      "expectedStatus": 401,
      "expectedBody": "string",
      "bodyMatcher": "contains"
    }
  ]
}